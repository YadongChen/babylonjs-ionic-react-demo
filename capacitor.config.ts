import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'babylonjs.ionic.react.demo',
  appName: 'babylonjs-ionic-react-demo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
